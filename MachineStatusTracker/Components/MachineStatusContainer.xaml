<UserControl x:Class="MachineStatusTracker.Components.MachineStatusContainer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MachineStatusTracker.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border  
             BorderBrush="{StaticResource BorderPrimary}" 
             SnapsToDevicePixels="True"
             CornerRadius="5"
             BorderThickness="2" >
            <Grid>
            <Grid.OpacityMask>
                <VisualBrush Visual="{Binding ElementName=border}"/>
            </Grid.OpacityMask>
                <Border
                x:Name="border"
               Background="White"               
                CornerRadius="5" />          


            <WrapPanel Background="WhiteSmoke" Orientation="Horizontal" >
                <local:MachineStatusView DataContext="Binding MachineStatusViewModel"></local:MachineStatusView>
              
            </WrapPanel>
            </Grid>
        </Border>

    </Grid>
</UserControl>
